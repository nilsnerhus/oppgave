% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_web.R
\name{scrape_web}
\alias{scrape_web}
\title{Scrape NAP documents from website}
\usage{
scrape_web(
  url = "https://napcentral.org/submitted-naps",
  name_col = 2,
  date_col = 6,
  link_col = 5,
  table_index = 1,
  has_header = TRUE,
  exclude_countries = NULL,
  output_path = "data/scraped_website.rds"
)
}
\arguments{
\item{url}{URL of the website to scrape (default: "https://napcentral.org/submitted-naps")}

\item{name_col}{Column index containing country names (default: 2)}

\item{date_col}{Column index containing publication dates (default: 6)}

\item{link_col}{Column index containing document links (default: 5)}

\item{table_index}{Index of the table to extract from the page (default: 1)}

\item{has_header}{Whether the table has a header row (default: TRUE)}

\item{exclude_countries}{Vector of country names to exclude (default: NULL)}

\item{output_path}{Path to save results (default: "data/scraped_website.rds")}
}
\value{
A list containing:
  \item{data}{
    \itemize{
      \item tokens - Data frame with doc_id and pdf_link for text extraction path
      \item metadata - Data frame with doc_id, country_name, and date_posted for metadata path
    }
  }
  \item{metadata}{Processing information including timestamp and statistics}
  \item{diagnostics}{Information about processing issues}
}
\description{
Scrapes the UNFCCC NAP Central website to extract National Adaptation Plans
  document links and metadata. Returns separate structures for text and metadata paths.
}
\examples{
\dontrun{
nap_data <- scrape_web(exclude_countries = c("Uruguay"))
# Access the two data paths
tokens_data <- nap_data$data$tokens
metadata_data <- nap_data$data$metadata
}
}
