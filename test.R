# Load your package
library(napr)

# If you're just checking the data structure
str(nap_data)

# Or to view the first few rows
head(nap_data)

# If you want to create a local copy for manipulation
my_data <- nap_data